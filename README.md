# Deep_neural_networks_utilities
this is a small Deep neural networks utilities written in pure python and numpy, it allows you to write different kindes of deep neural networks with fewer lines of code. 
